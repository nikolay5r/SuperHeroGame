#pragma once

namespace constants
{
	const size_t MAX_USERNAME_LENGTH = 20;
	const size_t MAX_PASSWORD_LENGTH = 16;
	const size_t MAX_NICKNAME_LENGTH = 16;
	const size_t MIN_USERNAME_LENGTH = 5;
	const size_t MIN_PASSWORD_LENGTH = 5;
	const size_t MIN_NICKNAME_LENGTH = 3;
	const unsigned MAX_POWER = 50;
	const unsigned MIN_POWER = 5;
}